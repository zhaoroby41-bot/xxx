# 小红书文案生成提示词模板 — 设计文档

**创建日期**：2026-08-12  
**最后更新**：2026-08-12  
**状态**：已实施 · HTML 模版 v1.0 完成  
**适用项目**：Apple 经销商矩阵小红书运营  
**输出文件**：`小红书文案生成报告模版.html`

---

## 一、背景与目标

### 背景

现有工作流中，运营人员每周通过「矩阵笔记内容分析提示词模板」生成小红书分析报告（HTML 格式），报告包含爆款归因、用户心智洞察、下周行动指南等 5 个维度的 insight。

目前 insight 只停留在「建议层面」，运营人员需要自行将 insight 转化为实际文案，效率低、一致性差，且文案质量依赖个人经验，缺乏数据支撑。

### 目标

设计一套「小红书文案生成提示词模板」，让运营人员能够：
1. 将分析报告的 insight + 爆款样本 + 下阶段热点一次性输入给 AI
2. AI 自动解构爆款公式，再批量生成 15-20 条带三维归因标注的文案
3. 输出风格统一的 HTML 报告，支持多维筛选，直接用于分发给各经销商账号

---

## 二、使用者与使用场景

| 项目 | 说明 |
|------|------|
| 使用者 | 品牌方运营人员（非经销商侧） |
| 触发时机 | 每次分析报告生成后，运营人员决定下发文案时触发 |
| 输入来源 | ① 分析报告中的关键结论（复制粘贴）② 运营人员自行补充的 insight ③ 下阶段热点信息 |
| 输出用途 | 分发给各经销商/门店账号，直接发布或微调后发布 |
| 输出数量 | 每次 15-20 条，覆盖多种内容类型 |

---

## 三、整体流程

```
运营人员填写输入区
（insight 摘要 + 爆款样本 + 内容方向指令 + 下阶段热点）
        ↓
AI Step 1：爆款结构解构
（从爆款样本提炼标题公式 / 开头钩子 / 正文节奏 / 结尾 CTA）
        ↓
AI Step 2：文案批量生成
（结合 insight + 爆款公式 + 热点借势，生成带三维归因标注的文案）
        ↓
输出 HTML 文案报告（5 个区块）
概览 → 下阶段热点日历 → 爆款公式 → 文案库（多维筛选） → 使用说明
```

---

## 四、提示词模板输入区设计

### 4.1 输入区字段（运营人员填写）

```
【本周 Insight 摘要】
- 爆款内容特征：{例：高收藏笔记集中在「真实使用场景」，标题含数字+痛点词}
- 高效内容方向：{例：iPhone 国补 + 门店体验 组合效果最佳}
- 用户心智信号：{例：用户处于决策前期，收藏率高，需强化干货密度}
- 其他补充：{运营人员自行填写}

【爆款笔记样本】
（粘贴 3-5 条本周 Top 爆款，每条包含标题 + 正文全文）
样本1 / 样本2 / …

【下阶段热点清单】
（填写下 2-4 周内的重要热点事件）
- {例：9月上旬 iPhone 17 发布}
- {例：国补政策 8月底截止}
- {例：开学季 8月-9月初}

【本期内容方向指令】
- 主推品类：{例：iPhone 16 / AirPods / Mac}
- 主推场景：{例：国补活动 / 开学季 / 门店体验}
- 特殊要求：{例：避免价格敏感词}

【生成数量】
- 产品种草类：{例：7 条}
- 门店体验类：{例：5 条}
- 活动促销类：{例：6 条}
```

### 4.2 AI 处理逻辑

**Step 1：爆款结构解构**

| 要素 | 说明 | 示例 |
|------|------|------|
| 标题公式 | 标题词性结构和情绪模式 | [痛点词]+[数字]+[解决方案] |
| 开头钩子类型 | 前两行吸引眼球的方式 | 场景代入型 / 疑问悬念型 / 数据冲击型 |
| 正文节奏 | 内容展开的逻辑结构 | 痛点→方案→验证→行动 |
| 结尾 CTA | 引导用户行动的方式 | 收藏提醒型 / 评论互动型 / 转发裂变型 |
| Emoji 风格 | 使用密度和情绪倾向 | 高密度生活化 / 低密度专业感 |
| 高频关键词 | 反复出现的词汇信号 | 国补、授权店、以旧换新 |

**Step 2：文案批量生成逻辑**
- 选择最匹配内容类型的爆款公式
- 将 insight 中的内容方向填入公式框架
- 结合热点日历中的时效性事件做借势处理
- 每条文案同时标注三维归因（公式 / Insight / 热点）

---

## 五、每条文案的输出字段

### 5.1 文案主体

| 字段 | 说明 |
|------|------|
| 标题 | 含 Emoji，20 字以内 |
| 正文 | 含话题引入 / 主体内容 / 结尾 CTA，150-300 字 |
| 话题标签 | 5-8 个，含品类词 + 场景词 + 长尾词 |
| 配图方向建议 | 1 句话描述推荐的封面/配图风格 |

### 5.2 三维归因标注

原有的「互动方向」和「适用账号类型」两个字段已移除，改为以下三维归因，每个 badge 均可点击触发筛选：

| 维度 | 颜色标识 | 说明 | 示例 |
|------|----------|------|------|
| 爆款公式来源 | 蓝色 badge | 套用了哪个公式，可点击筛选同公式文案 | `公式 #1 痛点数字型` |
| Insight 依据 | 绿色 badge | 哪条 insight 驱动了这个方向，可点击筛选同 insight 文案 | `高收藏·真实痛点` |
| 热点借势 | 橙色 badge | 关联了哪个热点事件，无热点时标注「无特定热点」 | `iPhone 17 预热` |

---

## 六、HTML 报告五区块结构

### 区块一：概览

- 4 个 KPI 卡片：爆款样本数 / 提炼公式数 / 生成文案总数 / 内容类型覆盖数
- **Insight 发现**（始终展开，2×2 卡片网格）：每条 insight 展示数据依据 + 「驱动文案 →」关联标签，4条覆盖：高收藏·真实痛点 / 国补曝光效率 / 开学季收藏峰值 / 门店评论转化
- 两张图表并排：内容类型分布饼图 + 预期互动方向柱状图（Chart.js 4.4.0）

### 区块二：下阶段热点日历（新增）

- 6 张热点卡片，按时间紧急度分色（红=即将截止 / 橙=近期 / 蓝=中长期）
- 每张卡片包含：时间标签 / 热点类型 / 热点描述 / 内容策略建议 / 关联现有文案编号
- 覆盖热点：国补截止 / 开学季 / iPhone 17 发布 / Apple Watch S11 / AirPods 新款 / 双十一

### 区块三：爆款结构公式

- 2×2 网格，4 套公式卡片
- 每张展示：公式名称 / 来源样本 / 标题公式（tag 形式）/ 效果标注 / 6 格详情（开头钩子 / 正文节奏 / 结尾 CTA / Emoji 风格 / 标题模板 / 高频关键词）

### 区块四：文案库（主体）

**筛选器设计（4行维度）**

| 行 | 维度 | 选项 |
|----|------|------|
| 1 | 内容类型 | 全部 / 📱产品种草(7) / 🏪门店体验(5) / 🎁活动促销(6) |
| 2 | 📐 爆款公式 | 全部公式 / #1痛点数字 / #2体验分享 / #3活动紧迫 / #4开学种草 / #1+3融合 |
| 3 | 💡 Insight | 全部 / 高收藏·真实痛点 / 国补曝光效率 / 开学季收藏峰值 / 门店评论转化 |
| 4 | 🔥 热点借势 | 全部热点 / 国补截止 / 开学季 / iPhone 17预热 / Watch S11 / 双十一 / 无特定热点 |

**筛选逻辑**
- 4 个维度同时生效（AND 逻辑），任意组合
- 筛选结果实时显示「共 N 条」提示，带「✕ 清除筛选」入口
- 点击卡片底部归因 badge 可直接触发该维度筛选，并自动滚动到文案库
- 同一 badge 再次点击则重置该维度

**筛选面板样式**
- 整体用 `.filter-panel` 卡片包裹（深色背景 + 细边框 + 圆角16px）
- 每行用 `.filter-row`，行间用细分割线隔开
- 每行左侧有 `.filter-row-label` 标签说明维度
- 激活色按维度区分：内容类型=品牌红 / 公式=蓝 / Insight=绿 / 热点=橙

**文案卡片结构**
- 顶部彩色条（产品=红 / 门店=蓝紫 / 促销=粉紫）
- 序号 + 复制按钮（只复制标题+正文+标签，不含归因说明）
- 标题 / 正文 / 话题标签 / 配图建议
- 底部归因区（深色背景分隔）：三维 badge，每个可点击触发筛选

**卡片 data 属性**（用于 JS 筛选）
```
data-type="product|store|promo"
data-formula="1|2|3|4|1+3"
data-insight="痛点|国补|开学|门店"
data-hotspot="国补截止|开学季|iPhone17|Watch11|双十一|无"
```

### 区块五：使用说明

三张横向卡片：如何挑选文案 / 如何分发给经销商 / 发布前检查清单

---

## 七、视觉规范

| 项目 | 规范 |
|------|------|
| 背景 | 深色渐变 `#0f0c29 → #1a1a2e → #16213e`（与现有分析报告一致） |
| 主色调 | 小红书品牌红 `#ff2442` |
| 归因 badge | 蓝色=公式来源 / 绿色=insight依据 / 橙色=热点借势 |
| 热点日历时间标签 | 红色=即将截止 / 橙色=近期 / 蓝色=中长期 |
| 字体 | `-apple-system, BlinkMacSystemFont, 'SF Pro Display', 'PingFang SC'` |
| 图表库 | Chart.js 4.4.0（CDN 引入） |

---

## 八、JS 交互功能清单

| 函数 | 说明 |
|------|------|
| `applyFilter(btn)` | 筛选栏按钮点击，更新对应维度筛选状态，按维度使用对应颜色 active class，调用 runFilter |
| `badgeFilter(type, val)` | 归因 badge 点击，切换该维度筛选；再次点击重置；自动滚动到文案库顶部 |
| `resetFilter()` | 清除所有维度筛选，各维度「全部」按钮恢复对应颜色高亮 |
| `runFilter()` | 核心筛选执行：AND 逻辑匹配4维度，更新卡片显示/隐藏、提示条文字、badge 高亮 |
| `copyCard(btn, cardId)` | 复制文案主体（标题+正文+标签），不含归因说明；clipboard API + execCommand 降级 |
| `showCopied(btn)` | 复制按钮反馈动画，2秒后恢复原状 |

**维度与 active class 对应关系**

| 维度 | active class | 颜色 |
|------|-------------|------|
| type（内容类型） | `active` | 品牌红 #ff2442 |
| formula（爆款公式） | `active-formula` | 蓝色 #4facfe |
| insight（Insight） | `active-insight` | 绿色 #43e97b |
| hotspot（热点） | `active-hotspot` | 橙色 #fbbf24 |

---

## 九、文件命名规范

```
小红书文案库_{渠道}_{YYYYMMDD}.html
例：小红书文案库_online渠道_20260812.html
    小红书文案库_mono渠道_20260812.html
```

---

## 十、与现有工作流的关系

```
现有流程：
Excel 数据 → 分析报告提示词 → 小红书矩阵分析报告.html

新增流程（在现有流程之后）：
小红书矩阵分析报告.html（insight 来源）
        +
运营人员补充下阶段热点清单
        ↓
文案生成提示词模板
        ↓
小红书文案库.html（分发给各经销商）
```

两套提示词模板相互独立，文案生成模板是分析报告的下游，不修改现有模板。

---

## 十一、不在本次范围内

- 自动读取 HTML 报告并解析 insight（本次为手动复制粘贴）
- 文案自动推送给经销商（本次只生成 HTML，分发由运营人员手动完成）
- 抖音文案版本（本次只做小红书）
- 文案发布排期管理（在 HTML 内直接标注即可，不做独立工具）
